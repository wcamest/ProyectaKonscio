import React, { PropsWithChildren } from "react";
type Props = {};
const HTMLOptgroupComponent = (props: PropsWithChildren<Props>) => {
return <optgroup>{props.children}</optgroup>;
};
export default HTMLOptgroupComponent;
